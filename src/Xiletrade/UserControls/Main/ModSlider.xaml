<UserControl x:Class="Xiletrade.UserControls.Main.ModSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Xiletrade.UserControls.Main"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <!-- Temporary usercontrol / Used for design -->
    <UserControl.Resources>
        
    </UserControl.Resources>
    <Grid d:Background="{StaticResource LightBg}">
        
        <DockPanel Margin="45,35,0,0" VerticalAlignment="Top" Width="130">
            <TextBox DockPanel.Dock="Right" Height="22" Width="41"/>
            <Slider Margin="0,1,0,0" VerticalAlignment="Top" Width="80" Height="20" Value="50" Maximum="100" Style="{StaticResource sliderFlat}" />
        </DockPanel>
        
        <Grid>
            <Grid x:Name="grid_min" Height="22" Width="41" Margin="60,60,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="{Binding OptionVisible, Converter={StaticResource inverseBool2VisibilityConverter}}" IsEnabled="{Binding ElementName=select, Path=IsChecked}">
                <TextBox x:Name="min" HorizontalAlignment="Left" Height="22" Width="41" d:Text="10" VerticalAlignment="Top" Tag="textbox" FontFamily="Arial" TextAlignment="Center" Text="{Binding Min, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock x:Name="style_min" IsHitTestVisible="False" Text="min" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#FF555555" Width="41" TextAlignment="Center" FontSize="10">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=min}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <Grid x:Name="grid_max" Margin="103,60,0,0" Height="22" Width="41" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="{Binding OptionVisible, Converter={StaticResource inverseBool2VisibilityConverter}}" IsEnabled="{Binding ElementName=select, Path=IsChecked}">
                <TextBox x:Name="max" HorizontalAlignment="Left" Height="22" Width="41" d:Text="" VerticalAlignment="Top" Tag="textbox" FontFamily="Arial" TextAlignment="Center" Text="{Binding Max, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock x:Name="style_max" IsHitTestVisible="False" Text="max" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#FF555555" Width="41" TextAlignment="Center" FontSize="10">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=max}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>
        <Grid VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,90,0,0">
            <Slider Margin="60,0,0,0" Value="50" Maximum="100" Width="84" Style="{StaticResource sliderMod}">
            </Slider>
        </Grid>
    </Grid>
</UserControl>
